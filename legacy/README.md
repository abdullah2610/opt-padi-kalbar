# Legacy

Codebase awal opt-padi-kalbar (vanilla JS prototype). Disimpan sebagai referensi sampai migrasi ke `web/` (React + Vite + PWA) selesai. Akan dihapus setelah Phase 6 + 7.

## File

- `index.html`, `masukan.html`, `app.js`, `favicon.svg` — frontend lama
- Asal: `api/weather.js` (di root, masih aktif dipakai sampai diport ke `api/disease-risk.js`)
